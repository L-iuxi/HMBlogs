---
title: "中断机制 — 8259A 可编程中断控制器"
date: "2026-01-15T10:00:00+08:00"
tags: ["操作系统", "中断"]
title-images: []
ending-images: []
author: "烩面"
draft: false
table-of-contents: true
toc-auto-numbering: false
---
<!-- introduction -->
《操作系统真相还原》学习笔记：中断分类、中断描述符表 IDT、8259A 可编程中断控制器的工作原理与初始化。
<!--more-->

## 中断的定义

CPU 获知了计算机中发生的某些事，暂停正在执行的程序，转而去执行处理该事件的程序，处理完毕后继续执行刚才的程序。整个过程称为中断处理。

## 并发和并行

- **并发**：单位时间内的累积工作量（单核 CPU 谈并发）
- **并行**：真正同时进行的工作量（多核 CPU 谈并行）

## 中断的分类

### 外部中断（硬件中断）

来自 CPU 外部的中断，中断源必须是某个硬件。

![外部中断](ch7-image.png)

CPU 中有两条接收中断的信号线：

- **INTR**：可屏蔽中断，不影响系统运行，CPU 可以不处理
- **NMI**：不可屏蔽中断，致命错误，系统无法继续运行

### 内部中断（软中断）

由软件主动发起的中断。

## 中断描述符表 IDT

保护模式下用于存储中断处理程序入口的表。

### 门描述符

| 类型 | 说明 |
|------|------|
| 任务门 | 用于硬件任务切换，需配合 TSS |
| 中断门 | IF 位自动置 0，关闭中断嵌套 |
| 陷阱门 | IF 位不变，允许中断嵌套 |
| 调用门 | 用户进程进入特权 0 级的方式 |

除调用门外，任务门、中断门、陷阱门都可以存在于 IDT 中。

### lidt 指令

`lidt` 加载 48 位内存数据：前 16 位是 IDT 表界限，后 32 位是 IDT 线性基地址。

![lidt](ch7-image-1.png)

## 中断处理过程

### CPU 外部

外部设备的中断由中断代理芯片接收，处理后将该中断的中断向量号发送到 CPU。

### CPU 内部

1. 处理器根据中断向量号定位中断门描述符
2. 处理器进行特权级检查（检查 CPL 与目标代码段 DPL）
3. 执行中断处理程序

### 从中断返回

`iret` 指令从栈顶弹出数据到各寄存器，从中断处理程序返回。

## 8259A 可编程中断控制器

负责所有来自外设的中断，管理和控制可屏蔽中断，包括屏蔽外设中断、优先级判决、向 CPU 提供中断向量号。

![8259A](ch7-image-3.png)

| 组件 | 功能 |
|------|------|
| INT | 选出最高优先级中断，通知 CPU |
| INTA | 接受 CPU 的中断响应信号 |
| IMR | 中断屏蔽寄存器 |
| IRR | 中断请求寄存器，锁存等待处理的中断 |
| PR | 优先级仲裁器 |
| ISR | 中断服务寄存器，保存正在处理的中断 |

### 初始化 8259A

**ICW（初始化命令字）**：
- ICW1：设置连接方式和中断触发方式
- ICW2：设置起始中断向量号
- ICW3：级联配置（主从片互连）
- ICW4：工作模式设置

**OCW（操作命令字）**：
- OCW1：屏蔽外设中断信号
- OCW2：设置中断结束方式和优先级模式
- OCW3：特殊屏蔽方式及查询方式
